{"postCreateCommand": "dirName='Jhonatan_LAB' && dirNum='45' && newDir='${dirName}${num}' && mkdir '$newDir'",
  "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "GitHub.github-vscode-colorTheme"
            ],
            "settings": {
                "workbench.colorTheme": "GitHub Light Default"
            }
        }
    }
}
